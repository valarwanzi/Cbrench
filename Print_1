void 
PrintOut( unsigned int N )
{
  unsigned int res[11] = {0}; //N less than 1 billion
  unsigned int cnt;
  if( N >= 10 )
    PrintOut( N / 10);
  res[cnt] = N % 10; // N%10 = N-[N/10]*10
  cnt++;
}
